let options = {
    strings: ['husanboy assalomyu aleykum', 'vaaleykum assalom dostim'],
    typeSpeed: 60,
    startDelay: 500,
    backSpeed: 40,
    loop: true,
    loopCount: Infinity,
  };
  
  let typed = new Typed('.element', options);